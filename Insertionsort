def insertion_sort(arr):
  n = len(arr)

  for i in range(n):
    current = arr[i]
    j = i - 1
    while(arr[j] > current and j >= 0):
      arr[j + 1] = arr[j]
      j-=1
    arr[j + 1] = current

if __name__ == '__main__':
  arr = [5, 4, 3, 2, 1]
  insertion_sort(arr)
  print(arr)
